[package]
name = "auth-rs"
version = "0.1.0"
edition = "2024"

[features]
default = []
axum = ["dep:axum"]

[dependencies]
axum = { version = "0.8", optional = true }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = { version = "1.0.150" }

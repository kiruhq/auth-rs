[package]
name = "axum-auth"
version = "0.1.0"
edition = "2024"

[dependencies]
auth-rs = { path = "../../", features = ["axum", "sqlx"] }
axum = "0.8"
tokio = { version = "1", features = ["full"] }
sqlx = { version = "0.9", features = ["runtime-tokio"] }
